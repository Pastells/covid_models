%DATA
./data/italy.dat

%PARAMS
--n integer [10000,10000000]
--beta_i real [0.05,1.0]
--beta_a real [0.01,1.0]
--delta_i real [0.05,1.0]
--delta_a real [0.01,1.0]
--alpha real [0.05,2.0]

%MODULES
python/3.6.5

%META
%%SEED 42
%%TIMEOUT 1200

%ENVIRONMENT-SETUP
python3 -m venv ./venv
./venv/bin/pip install numpy

%EXECUTION
./venv/bin/python -u ./models/sair.py \
--data ${instance} \
--seed ${seed} \
--timeout ${timeout} \
--undiagnosed 0 \
${@}
